[pytest]
# Discover tests from the project root
testpaths = .
# Exclude legacy/experimental code and virtual environment
norecursedirs = Spikes .venv .git __pycache__
# Standard naming: test_*.py files only (not *_test.py legacy style)
python_files = test_*.py
python_classes = Test*
python_functions = test_*
